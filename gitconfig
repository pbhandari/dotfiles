[user]
    name             = Prajjwal Bhandari
    email            = pbhandari@pbhandari.ca
[github]
    user             = pbhandari

[apply]
    ignorewhitespace = change
    whitespace       = error

[credential]
    helper           = cache --timeout=3600

[core]
    editor           = vim
    autocrlf         = input
    filemode         = false
    whitespace       = trailing-space,space-before-tab

[merge]
    log              = true
    tool             = gvimdiff

[push]
    default          = upstream

[color]
	ui               = true
[color "branch"]
    current          = green bold
    local            = blue
    remote           = yellow
[color "decorate"]
    branch           = blue bold
    remoteBranch     = green
    tag              = green bold

[column]
    ui               = auto

[diff]
    renames          = true
    algorithm        = patience

[pager]
    l                = false

[grep]
    lineNumber       = true
    patternType      = perl

[alias]
    l                = log -n20 --pretty=sparselog --date=relative
    up               = pull origin
    s                = status -sb
    st               = status
    di               = diff
    co               = checkout
    ci               = commit
    br               = branch
    sta              = stash
    assume           = update-index --assume-unchanged
    unassume         = update-index --no-assume-unchanged
    assumed          = "!git ls-files -v | awk '/^h/ {print $2;}'"
	ds               = diff --staged

[pretty]
    sparselog        = format:\
%C(auto)%h%C(reset) -%C(auto)%d %C(reset)%s %C(cyan)(%cd)%C(reset)
    full-log         = tformat:\
Commit: %Cred%h%Creset%C(yellow)%d%Creset (%H)%n\
Author: %aN %C(blue bold)<%ae>%Creset%n\
Date  : %C(green)%ad%Creset%n\
%n\
%s\
%n\
%n\
%-b\
%n
